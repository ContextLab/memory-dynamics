# drivingdynamics
Binder for drivingdynamics project

# To use:
1. `git clone https://github.com/ContextLab/drivingdynamics.git`
2. navigate to the folder and then `pip install -r requirements.txt`
3. then `jupyter notebook drivingdynamics.ipynb`
