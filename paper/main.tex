\documentclass[10pt]{article}
\usepackage{amsmath}
\usepackage[english]{babel}
\usepackage[font=small,labelfont=bf]{caption}
\usepackage{geometry}
\usepackage[sort&compress]{natbib}
\usepackage{pxfonts}
\usepackage{graphicx}
\usepackage{setspace}
\usepackage{hyperref}
\usepackage{lineno}
\usepackage{xcolor}
%\usepackage{placeins}
%\usepackage[nolists, nomarkers, fighead]{endfloat}
%\renewcommand{\includegraphics}[2][]{}

\setcitestyle{notesep={; }}

% supplementary methods
\newcommand{\suppMethods}{\textit{Supplementary methods}}
% supplementary figures
\newcommand{\kOptimization}{1}
\newcommand{\recallEventseg}{2}
\newcommand{\recallTrajectories}{3}


\newcommand{\comment}[1]{}

\NewCommandCopy{\oldnameref}{\nameref}
\renewcommand{\nameref}[1]{\textit{\oldnameref{#1}}}

\doublespacing
\linenumbers

% small hack to fit the full abstract on the first page
\title{\vspace{-2em}Delayed recountings preserve but simplify the semantic geometry of earlier recountings}

\author{Paxton C. Fitzpatrick, Alishba Tahir, Jennifer Xu, Soo Hwan Park,\\and Jeremy R. Manning\textsuperscript{*}\\
\vspace{-0.5em}\small{Department of Psychological and Brain Sciences}\\
\small{Dartmouth College, Hanover, NH 03755, USA}\\
\vspace{-0.5em}\small{\textsuperscript{*}Corresponding author: Jeremy.R.Manning@Dartmouth.edu}}

\date{}

\begin{document}
\maketitle

\begin{abstract}\noindent
What is a second recounting of an experience a recounting \textit{of}?
Memories change with time and with retrieval, so each recounting may be shaped less by the experience itself than by how we last recounted it.
Here we use a geometric framework to compare two recountings of the same experience.
We model experiences and recountings as trajectories through a semantic embedding space, then segment each trajectory into discrete events.
We apply our approach to data collected from participants who watched a television episode, verbally recounted it immediately afterward, then recounted it again one week later.
We find that the trajectories of participants' delayed recountings resemble their own immediate recountings more closely than they resemble the episode itself.
We also find that this resemblance is person-specific enough to identify one of an individual's recountings of the experience from the geometry of their other recounting.
The delayed recountings' trajectories were also well described as geometrically simplified versions of immediate recountings' trajectories, in that the immediately recounted events that least contributed to a trajectory's shape were the events most likely to be omitted from the delayed recounting.
These findings suggest that the act of retrieving a memory after a delay draws on a person-specific distillation of the experience, rather than the original experience as it was first encountered.
Further, the distillation itself appears to proceed in a way that best preserves the shape of the original recounting's trajectory.
\end{abstract}


\section*{Introduction}

Suppose you attend a friend's wedding.
That evening, you describe it to your partner, recounting how the ceremony ran long, how the best man's toast was unexpectedly moving, and how a small child wandered onto the dance floor midway through the first song.
A week later your coworker asks how the wedding was, and you tell the story again.
What specifically are you recounting to your coworker?
Is it the wedding itself, as you experienced it?
Or is it the \textit{story} about the wedding you told your partner the week before?
Or is it perhaps some combination of the two?
Further, if the two recountings differ, do their differences solely reflect the ordinary imprecision of putting an experience into words?
Or is it that the representation itself has changed?

An experience is already transformed well before anyone sets out to describe it.
Our brains do not maintain an exhaustive account of everything we encounter, so the very act of perceiving an experience entails compressing and distorting it.
We attend closely to some aspects of a scene while ignoring others, and we tend to interpret ambiguous moments in light of what has happened previously and what we expect will happen next.
But once an experience has ended, neither the experience itself nor our moment-to-moment perception of it remains available, to us or to anyone else.
Rather, what remains is the product of a further distillation through which our perception of the experience is reduced to something our memory systems can store and later reconstruct.
Which aspects of an experience survive this second distillation may depend on what we attended to; what we anticipated would matter later; what we found interesting; and the thoughts, motivations, and biases we brought with us.
Because remembering is reconstructive, the resulting account often preserves the general ``shape'' of what happened while discarding many of its particulars~\citep{Bart32,SchaEtal11,Scha12}.
The units over which these distillations operate appear to be events.
People segment continuous experience into discrete episodes at moments of change, and those boundaries shape what is remembered afterward~\citep{RadvZack17,BaldEtal17}.
In prior work we characterized this transformation geometrically by modeling an experience and a person's recounting of it as trajectories through a semantic space, and asking which properties of the experience's trajectory are preserved in the recounting~\citep{HeusEtal21}.

After encoding an experience into memory, the representation of that memory is not fixed.
Information may be lost over time; experiences we have after an event may color how we later remember it; similar experiences may interfere with one another; and we may simply re-evaluate what happened in light of what we have learned since.
Even the act of retrieving a memory itself appears to be one of the forces that changes it, in that a memory can become labile when it is reactivated and must be stabilized again afterward~\citep{NadeEtal00,HupbEtal07,HupbEtal09,SedeEtal11}.
Retrieval also changes what will be accessible later relative to memories that go unretrieved~\citep{RoedKarp06a,KarpRoed07}.
The particular way in which we recount an experience appears to matter as well, since people who are asked to describe an event for one purpose later remember it in ways that are biased toward that description~\citep{TverMars00}.
Memories become more semantic and less perceptually detailed with the passage of time and with repeated rememberings~\citep{LifaEtal21}.
Repeated retrievals of naturalistic events over the course of a week also seem to stabilize each person's recall (for example, which details they include) without causing different people's recalls to converge~\citep{MeleEtal25}.
Taken together, these observations suggest that each time we remember an experience, the substance of the underlying memory we are retrieving differs from what we retrieved before.
Further, the \textit{direction} of these distortions appears to be idiosyncratic across people.

How might we compare two recountings of the same experience, when the experience and both recountings take the form of unconstrained natural language?
Recent work has begun to use language models to quantify the content of what individuals say when they recount what they remember~\citep{HeusEtal21,MannEtal22,FitzEtal26,FeneEtal25}.
The approach developed in our prior work embeds the description of an experience, and each individual's recounting of it, into a common high-dimensional space whose coordinates reflect the concepts under consideration, and through which the experience and recountings each unfold over time as a geometric trajectory~\citep{HeusEtal21,FitzEtal26,VeraEtal25}.
We then segment the experience and each recounting into discrete events and identify which event from the experience each recall event describes, yielding sequences of events that may be compared directly across people and across delays~\citep{ChenEtal17,BaldEtal17,HeusEtal21}.

Here we apply our approach to data from an experiment in which participants viewed an episode of a television show, recounted its events immediately afterward, and then recounted them again following a week-long delay.
We are particularly interested in comparing both recountings to each other and to the original experience.
For example, if retrieval always recovers the original experience, perhaps with some independently drawn corruption (noise), then on average both recountings should be about equally similar to the episode.
We find instead that the structure of a participant's delayed recounting is more similar to that of their own immediate recounting than to that of the original episode.
It is also more similar to their own immediate recounting than to other participants' immediate recountings, to the degree that one of an individual's two recountings can be identified from the other on the basis of this structure alone.
Retrieval after a delay therefore appears to draw on a person-specific distillation of the episode more than on the episode as it was first experienced.

Capturing the semantic geometry of individuals' recountings at different points in time also offers the opportunity to characterize the transformations undergone by our memories beyond an initial recounting.
For example, if individuals' recountings tend to retain their idiosyncratic structures after a delay, how \textit{do} the structures of our memories tend to change with time?
There is a particular qualitative change our memories undergo as we naturally forget aspects of experience, implied by common descriptors such as ``fading'', ``blurring'', becoming ``fuzzy'', or ``decaying''~\citep{Thor13}.
But the nature of this change can be difficult to characterize more concretely.
Here we show that this transformation is well described as geometrically simplifying the path through semantic space an individual follows as they recount an experience.
We consider a method for simplifying geometric curves, originally developed for use constructing cartographic maps at different scales, expressing different levels of detail~\citep{Rame72,DougPeuc73}.
When we apply this method to the trajectory through semantic space traced out by a participant's immediate recounting, we can quantify how strongly each of their recall events contributes to defining the trajectory's overall structure.
We find that this structural importance predicts which events a participant will retain versus omit from their recounting following a week-long delay.


\section*{Results}

We asked participants to view and verbally recount episodes of television shows across two experiment sessions, separated by approximately one week (Fig.~\ref{fig:paradigm}; see \nameref{subsec:experiment}).
In the first session, participants viewed the pilot episode of \textit{Atlanta}.
They were then given up to an hour to recount what had happened in the episode to the best of their abilities (i.e., \textit{immediate recall}).
After they finished recounting the episode, we asked participants to verbally predict what they thought would happen in the show's \textit{next} episode.
Participants then returned for a second session between 6 and 8 days later, where they began by again verbally recounting the \textit{Atlanta} episode they had viewed the week prior (i.e., \textit{delayed recall}).
Following the delayed recall phase, participants viewed and recounted either the second episode of \textit{Atlanta} or the pilot episode of a different TV series, \textit{Arrested Development}.
We designed the experiment to support exploring a range of memory phenomena beyond those we consider here.
In the present study, we focus specifically on characterizing how the contents and structures of participants' recountings of the first \textit{Atlanta} episode changed over the week-long delay.

\begin{figure}[t]
    \centering
    \includegraphics[width=\textwidth]{figures/source/paradigm}

    \caption{\textbf{Experimental paradigm.} Visual timeline of the two study sessions, spanning approximately one week.
    In session 1, participants view the pilot episode of \textit{Atlanta}, immediately verbally recount its content, and then verbally predict what will happen in the next episode.
    In session 2, participants again recount the \textit{Atlanta} episode (viewed the week prior), then view and verbally recount either the second episode of \textit{Atlanta} or the pilot episode of \textit{Arrested Development}.
    Here we analyze participants' immediate and delayed recalls of the first \textit{Atlanta} episode.
    Faded elements denote phases of the experiment that are not analyzed in the present study: the prediction phase at the end of session 1, and the second episode (and its recounting) in session 2.}
    \label{fig:paradigm}
\end{figure}

To characterize how participants preserved and distorted the content of the episode in their recountings, we adapted an approach developed in our prior work that casts experiences and memories as \textit{trajectories} unfolding in a common high-dimensional space~\citep{HeusEtal21,FitzEtal26}.
We construct these trajectories using a text embedding model, which transforms natural language text into a vector representation that captures the text's semantic meaning.
Intuitively, this vector may be treated as a coordinate in a semantic space, in which texts with similar meanings are assigned to nearby coordinates while those with diverging meanings are assigned to more distant coordinates. 
In this way, the distance between two texts' embedding coordinates reflects how semantically similar their contents are.
We created detailed annotations describing the content of each individual camera shot throughout the \textit{Atlanta} episode (Fig.~\ref{fig:pipeline}A; see \nameref{subsec:annotations-transcripts}) and used EmbeddingGemma~\citep{VeraEtal25} to transform this sequence of annotations into a sequence of text embedding coordinates.
We then transformed the transcript of each participant's recounting from each of the two experiment sessions into an analogous sequence of coordinates (Fig.~\ref{fig:pipeline}B,C).
The progression of coordinates in each of these sequences traces out a trajectory, yielding one path through the embedding space for the episode, and one for each recounting (Fig.~\ref{fig:pipeline}D--F; see \nameref{subsec:text-embedding}).

\begin{figure}[t!]
    \centering
    \includegraphics[width=\textwidth]{figures/source/pipeline-overview}

    \caption{\small \textbf{Modeling the geometry of experiences and memories.}
    Each row illustrates the construction of one of the three sequences we compare: the \textit{Atlanta} episode (top), and an example participant's immediate (middle) and delayed (bottom) recountings of it.
    Each column shows one stage of our modeling pipeline, applied to all three sources in turn.
    \textbf{A}--\textbf{C.} \textit{Text.} Hand-written annotations describing the content of an example camera shot shown to the left (\textbf{A}), and an excerpt from an example participant's immediate and delayed recall transcripts describing that same content (\textbf{B},\textbf{C}).
    \textbf{D}--\textbf{F.} \textit{Trajectories.} Each text snippet is embedded as a coordinate in a 768-dimensional semantic space, and successive coordinates trace out a trajectory.
    Color denotes elapsed time within each sequence (red: early; blue: late).
    For display, we projected the 768-dimensional trajectories onto two dimensions using UMAP~\citep{McInEtal18a}; all analyses we report were carried out in the original space.
    \textbf{G}--\textbf{I.} \textit{Event-segmentation.} For the episode, the correlation between the embedding vectors at each pair of timepoints, with the boundaries of the 20 events recovered by the hidden Markov model outlined (\textbf{G}).
    For each recounting, the cosine similarity between every recall window and every episode event, with the decoded sequence of episode events described outlined (\textbf{H},\textbf{I}).
    Darker heatmap colors denote higher similarities.
    Analogous plots for all participants are shown in Supplementary Figure \recallEventseg.
    \textbf{J}--\textbf{L.} \textit{Events.} The resulting sequences of events, each summarized by averaging the embedding vectors it spans, and joined in the order they occur.
    \textbf{G}--\textbf{L.} Episode events are colored by their temporal order (red: early; blue: late) and each recall event is colored according to the episode event it describes.}
    
    \label{fig:pipeline}
\end{figure}

Neither the episode's trajectory nor participants' recall trajectories move through the space at a constant rate.
Both tend to linger within a given region for a period of time before shifting relatively abruptly to another. 
We treated these periods of relative stability as \textit{events}~\citep{RadvZack17,EzzyDava11} and used a hidden Markov model~\citep[HMM;][]{BaumPetr66,BaldEtal17,KumaEtal21} to identify boundary points in the episode's trajectory where the episode content abruptly shifted (Fig.~\ref{fig:pipeline}G).
This yielded a sequence of 20 events, each of whose content we summarized by averaging the embedding vectors across the timepoints it spanned (Fig.~\ref{fig:pipeline}J; see \nameref{subsec:eventseg}).
We then sought to identify whether, when, and how each participant described these different events in their verbal recountings.
We therefore treated the episode's 20 events as a fixed set of states in a separate HMM and decoded the boundary points in each recall trajectory where the participant shifted from (most probably) describing one event in the episode to describing another (Fig.~\ref{fig:pipeline}H,I).
As with the episode, we then summarized each participant's description of each event by averaging the embedding vectors between successive boundaries (Fig.~\ref{fig:pipeline}K,L).
In summary, this procedure yielded (a) a sequence of embedding vectors characterizing the events from the episode, (b) sequences of embedding vectors characterizing each participant's immediate and delayed recall, and (c) a mapping from every recall event onto the episode event it describes.

Our high-level goal is to characterize the structures of participants' recountings of the episode, and the degree to which they remained stable versus changed over the week-long delay.
The simplest sense in which a recounting has structure is the one that traditional list-learning studies measure: which of the episode's events a participant recalled, and in what order.
Participants described an average of 16.981 of the episode's 20 events in the immediate session and an average of 16.264 of them after the delay (a reliable decline; mean difference = 0.717 events, $\text{95\% confidence interval (CI)} = \left[0.226,~1.189\right]$, Wilcoxon's $T_W = 199$, $p = 0.007$, rank-biserial $r_{rb} = 0.490$).
Their recountings also comprised fewer events overall, falling from 18.245 events immediately to 17.170 a week later (mean difference = 1.075 events, $\text{95\% CI} = \left[0.358,~1.774\right]$, $T_W = 321.5$, $p = 0.003$, $r_{rb} = 0.475$).
Measured this way, a week of delay costs a participant roughly seven tenths of one event out of 20.

We next sought to characterize the \textit{order} in which participants described the episode's events.
In general, participants tended to recount the episode's events in roughly the order they occurred, and tended to describe similar sets of events in both sessions.
We characterize these phenemena formally below, but they are worth noting here because this tendency imposes a constraint on the ordering of events that any given recounting is likely to produce.
In particular, when we compare two recountings to each other, we need to account for the fact that the sequences of events they describe are not drawn from a uniform distribution over all possible orderings.
This means that the \textit{absolute} similarity of any two recountings' event-orderings is less informative than their \textit{relative} similarity within an appropriate distribution of alternative comparisons.
Given this, we developed a surrogate-based procedure that tested whether a participant's two recountings of the episode were \textit{more} similar in their event-orderings than we would expect under this shared constraint.
For every pair of episode events a given recounting described, we computed the proportion of times the event that occurred earlier in the episode was described before the event that occurred later. 
(This enabled us to capture when participants described the episode's events in order versus out of order, and when they later revisited an event they had already described; see~\nameref{subsec:precedence}.)
We then quantified the agreement between two different recountings as the mean absolute difference between these ``order precedence'' scores for each pair of events described in both recountings (Fig.~\ref{fig:recall-order}A--C).
We computed the order agreement between each participant's delayed recounting and all 53 participants' immediate recountings.
We then ranked each within-participant order agreement within its distribution of across-participant agreements, and tested these percentile ranks against chance.
In this way, each participant serves as their own control, and baseline ordering tendencies the episode's own ordering imposes on everyone are absorbed into the null distribution.
The same logic applies to each of the comparisons we report below.
These comparisons revealed that participants' delayed recountings tended to reflect the ordering structures of their own immediate recountings more closely than they did other participants' immediate recountings (mean percentile rank = 0.639, $\text{95\% CI} = \left[0.563,~0.710\right]$, $T_W = 354.5$, $p = 0.001$, $r_{rb} = 0.505$; Fig.~\ref{fig:recall-order}D,E).

\begin{figure}[tp]
    \centering
    \includegraphics[width=0.98\textwidth]{figures/source/order-similarity}

    \caption{\small \textbf{Recall order is preserved between immediate and delayed sessions.} 
    \textbf{A.} How order agreement is measured, illustrated with a schematic episode of six events. 
    For each recall sequence we compute, for every pair of episode events, the proportion of orderings in which the earlier event preceded the later event.
    The two recalls are then compared over the event-pairs described in both. 
    Cells are grey where an event went unrecalled, and hatched where a pair was not shared by the two recalls being compared. 
    \textbf{B.} The same pairwise precedence matrices computed from one participant's immediate and delayed recalls. 
    \textbf{C.} The contrast our analysis tests, comparing that same participant's delayed recall against a different participant's immediate recall. 
    \textbf{D.} For each participant, the order agreement between their delayed recall and their own immediate recall (pink) versus the median agreement between their delayed recall and the other 52 participants' immediate recalls (grey). 
    Each joined pair of dots is one participant.
    \textbf{E.} The proportion of participants whose own immediate recall falls among the $k$ most similar of all 53 in order agreement, as a function of $k$ (pink), against the proportion expected by chance (grey, dashed). 
    \textbf{A}--\textbf{C.} Color denotes precedence, from 0 (the second event always preceded the first) to 1 (the first always preceded the second).}
    
    \label{fig:recall-order}
\end{figure}

Both of the above measures (i.e., overall recall and order agreement) treat a recounting as a sequence of discrete items.
While this connects our approach to the long history of list-learning studies, it also fails to capture the richness of what individuals actually do when they recount an experience.
As one example, participants often returned to recount already-described events to provide additional detail.
On average, they produced more total recall events than the number of distinct episode events they described (18.245 vs.\ 16.981 in the immediate session; 17.170 vs.\ 16.264 in the delayed session), indicating that they ``revisited'' an average of about one event per recounting.
But this is different from simple repetition, since the \textit{ways} in which the same event is described across those retellings often differ in terms of which specific details are included, how the description of one event relates to the descriptions of others, and so on.
The same is true of different \textit{participants'} recountings of the same event.
Whereas two participants recalling the same item in a traditional list-learning study implies that they recalled the exact same content, two individuals recounting the same real-world event may differ in the particular details they emphasize and de-emphasize, how they connect it to other events in their memories, and so on.
By embedding the episode and each recounting into a common semantic space, we can capture these differences in a way that is not possible when treating them as lists of discrete items.
Viewed under this geometric lens, individual instances of one participant's event recountings may be quantitatively distinguished or compared (within or across participants, to the original episode's events, etc.).
Further, the shape of a recounting's path through the semantic embedding space (i.e., it's \textit{trajectory}; Fig.~\ref{fig:trajectories}) characterizes how the recounting unfolds over time, and how each recounted event relates to each other.

\begin{figure}
    \centering
    \includegraphics[width=\textwidth]{figures/source/2d-trajectories}

    \caption{\small \textbf{2D projections of the episode and recall trajectories.}
    The episode's event sequence (top left), together with the immediate (left) and delayed (right) recall event sequences for six example participants.
    Each colored dot denotes an event; solid black lines denote transitions between adjacent events.
    The episode's events are colored by their temporal order (red: early; blue: late) and each recall event is colored to match to the episode event it describes.
    The episode's shape is drawn beneath each recall as a dashed grey line for reference.
    We projected the episode and recall events onto two dimensions using UMAP for visualization; all analyses we report were carried out in the full 768-dimensional space.
    Analogous recall trajectory plots for all participants are shown in Supplementary Figure \recallTrajectories.}
    \label{fig:trajectories}
\end{figure}

In order to compare and contrast these trajectories' shapes, we used dynamic time warping~\citep{Vint68,SakoChib78}.
Intuitively, dynamic time warping finds the optimal alignment between two trajectories by locally ``stretching'' and ``compressing'' them to map each point along one trajectory onto one or more consecutive points along the other, while preserving both trajectories' temporal orderings (see~\nameref{subsec:dtw}).
The quality of this optimal alignment is summarized by its ``warp cost,'' or the residual distance between matched points along the trajectories after bringing them into optimal alignment.
In other words, the warp cost for a pair of trajectories reflects how closely they can be aligned by temporally stretching and compressing them, such that two trajectories that trace out the same shape will have a low warp cost even if they sample that shape at different resolutions.
Two trajectories that trace out different shapes, meanwhile, cannot be coerced into as close an alignment and will exhibit a higher warp cost.
We note that since warp cost is a \textit{cost} measure, it runs in the opposite direction to the above order similarity measure: a lower warp cost denotes greater similarity between two trajectories' structures, and vice versa.
Taken together, framing experiences and recountings geometrically and applying geometric measures to relate them enables us to formally quantify how similarly their qualitative semantic contents unfold.
We next leverage these tools to answer fundamental questions about how memories change over time.

We began our study with the question of what a delayed recounting is a recounting \textit{of}.
With the above tools in hand, we can now distinguish between two reasonable hypotheses.
One hypothesis (Fig.~\ref{fig:hypotheses}A) is that each time we recount an experience, what we produce is essentially a noisy version of the original.
Noise may increase over time (e.g., a delayed recounting may be \textit{further} from the original experience than an immediate recounting), but the underlying representation being retrieved remains consistent.
According to this hypothesis, on average, participants' delayed recountings should tend to align with the episode about as closely as they align with their own immediate recountings of it (Fig.~\ref{fig:hypotheses}B).
A second hypothesis (Fig.~\ref{fig:hypotheses}C) is that each time we recount an experience, what we produce is a noisy version of a previous \textit{recounting} of the original experience.
According to this alternative hypothesis, on average, participants' delayed recountings should tend to align more closely with their own immediate recountings than with the episode itself (Fig.~\ref{fig:hypotheses}D).

\begin{figure}
    \centering
    \includegraphics[width=\textwidth]{figures/source/hypotheses}

    \caption{\small \textbf{Simulations under two hypothetical accounts of memory retrieval.}
    \textbf{A},\textbf{B.\ Hypothesis 1.} A participant's immediate (red) and delayed (blue) recountings are both noisy versions of the original experience (black).
    \textbf{A.} Each dot represents the position of a semantic embedding trajectory, mapped into a two-dimensional space. 
    The Gaussian halos around the large dots denote drift and measurement noise. 
    The small red and blue dots show \textit{potential} simulated recountings (drawn from their respective sampling distributions), and the larger black-outlined dots show the simulated \textit{observed} recountings. 
    The horizontal grey bar denotes the length of one distance unit.
    \textbf{B.} The density plots display the distributions of distances (analogous to warp costs used in our main analyses) between the recountings and episode.
    Of note, the delayed and immediate recountings tend to be farther from each other than either the delayed or immediate recountings are from the original episode.
    \textbf{C},\textbf{D.\ Hypothesis 2.} A participant's delayed recounting is a noisy version of their own immediate recounting, rather than the original episode. 
    \textbf{C.} Same format as Panel~A, but here the delayed recountings are drawn from a distribution centered on the observed immediate recounting for this participant.
    \textbf{D.} Same format as Panel~B. Note that the delayed recountings are now much closer to the immediate recountings than they are to the original episode.
    \textbf{All panels.} Note that all data in this figure are simulated for the purposes of illustration.}
    \label{fig:hypotheses}
\end{figure}

Within our framework, distinguishing between the two accounts illustrated in Figure~\ref{fig:hypotheses} becomes a matter of comparing two sets of warp costs: between participants' delayed recountings and their own immediate recountings, and between their delayed recountings and the episode itself.
If retrieving a memory after a delay recovers the original experience (Hypothesis 1), then a participant's delayed recounting should align with the episode at least as closely as it aligns with their own earlier recounting of it.
If, however, retrieving a memory after a delay recovers a previous recounting of the experience (Hypothesis 2), then a participant's delayed recounting should align more closely with their own immediate recounting than with the episode itself.
We found clear support for this second hypothesis.
In particular, participants' delayed recountings aligned more closely with their own immediate recountings (mean warp cost 0.532; Fig.~\ref{fig:dtw}A) than with the episode itself (0.654; mean difference = 0.122, $\text{95\% CI} = \left[0.104,~0.139\right]$, $T_W = 1$, $p < 0.001$, $r_{rb} = 0.999$; Fig.~\ref{fig:dtw}B,D).
We found that this pattern was highly consistent, holding for all 53 participants in our dataset but one (for whom the absolute difference between their delayed-to-immediate and delayed-to-episode warp costs was the smallest of any participant).
This suggests that \textit{what} participants recounted after a week is better characterized as a recounting of their own (previous) immediate recounting than as a recounting of the actual original episode that they experienced.

\begin{figure}[p]
    \centering
    \refstepcounter{figure}
    \phantomsection
    \includegraphics[width=\textwidth]{figures/source/dtw-figure}
    \caption*{\small \textit{(Caption next page.)}}
    \label{fig:dtw}
\end{figure}

\begin{figure}[t]
    \ContinuedFloat
    \caption{\small \textbf{Participants' delayed recalls resemble their own immediate recalls.}
    \textbf{A.} An example participant's delayed recall trajectory (blue) aligned to their immediate recall trajectory (green) via dynamic time warping (DTW). 
    Grey lines connect DTW-matched events; the warp cost of the alignment (i.e., mean distance between matched events) is displayed in the top left.
    The $\searrow$ indicates the direction and magnitude of the offset applied to the trajectory in back for visual clarity.
    Each trajectory was projected to 2D for visualization using UMAP; DTW alignments, warp costs, and all statistical tests were computed in the original space.
    \textbf{B.} The example participant's delayed recall trajectory aligned to the episode's trajectory (orange), in the same format as Panel A.
    \textbf{C.} The example participant's immediate recall trajectory aligned to the episode's trajectory, in the same format as Panel A.
    \textbf{D.} The warp costs of aligning each participant's delayed recall to their immediate recall (teal) and to the episode (gold).
    Grey lines connect the paired costs for each participant; black bars denote the mean of each cost distribution.
    \textbf{E.} The warp cost of aligning each participant's delayed (gold) and immediate (slate) recall to the episode, in the same format as Panel D.
    \textbf{F.} The example participant's delayed recall trajectory aligned to a different participant's immediate recall trajectory (pink), in the same format as panel A.
    \textbf{G.} For each participant, the warp cost of aligning their delayed recall trajectory to their own immediate recall trajectory (pink) versus the median cost of aligning their delayed recall trajectory to the other 52 participants' immediate recall trajectories (grey), in the same format as Fig.~\ref{fig:recall-order}D.
    \textbf{H.} The proportion of participants for whom their own immediate recall trajectory falls among the $k$ most similar (i.e., lowest warp costs) to their delayed recall trajectory, as a function of $k$ (pink), against the proportion expected by chance (grey, dashed).
    \textbf{I.} The example participant's immediate recall trajectory aligned to a different participant's delayed recall trajectory (purple), in the same format as panel A.
    \textbf{J.} For each participant, the warp cost of aligning their immediate recall trajectory to their own delayed recall trajectory (purple) versus the median cost of aligning their immediate recall trajectory to the other 52 participants' delayed recall trajectories (grey), in the same format as Fig.~\ref{fig:recall-order}D.
    \textbf{K.} The proportion of participants for whom their own delayed recall trajectory falls among the $k$ most similar (i.e., lowest warp costs) to their immediate recall trajectory, as a function of $k$ (pink), in the same format as Panel H.}
    
\end{figure}


That delayed recountings more closely resemble immediate recountings than the original episode suggests that the memories reflected by those recountings might drift over time.
Accordingly, we found that participants' recountings came to align less closely with the original episode following the week-long delay (mean immediate-episode warp cost = 0.626; mean delayed-episode warp cost = 0.654; mean difference = 0.028, $\text{95\% CI} = \left[0.013,~0.042\right]$, $T_W = 288$, $p < 0.001$, $r_{rb} = 0.598$; Fig.~\ref{fig:dtw}B,C,E).
We next asked whether the nature of this drift was idiosyncratic to each participant or potentially reflected more general patterns shared across people.
To answer this question, we aligned each participant's delayed recounting to their own immediate recounting (Fig.~\ref{fig:dtw}A) and to all 52 other participants' immediate recountings (Fig.~\ref{fig:dtw}F), and compared the resulting warp costs.
As in our order agreement analysis, we ranked the warp cost for each within-participant alignment within its corresponding distribution of warp costs for across-participant alignments, and tested these percentile ranks against chance.
We found that every participant's delayed recounting aligned more closely with their own immediate recounting than would be expected by chance (mean percentile rank = 0.971, $\text{95\% CI} = \left[0.950,~0.987\right]$, $T_W = 0$, $p < 0.001$, $r_{rb} = 1.000$; Fig.~\ref{fig:dtw}G), with most participants' own immediate recountings yielding the single closest alignment of any immediate recounting, and nearly all participants' own immediate recountings falling within the top few closest (Fig.~\ref{fig:dtw}H).
The same held (for all but two participants) when we performed the same comparison in the opposite direction, aligning each immediate recounting to the full set of delayed recountings (mean percentile rank = 0.923, $\text{95\% CI} = \left[0.874,~0.965\right]$, $T_W = 11.5$, $p < 0.001$, $r_{rb} = 0.984$; Fig.~\ref{fig:dtw}A,I,J,K).
Taken together, these results suggest that the specific ways in which participants' recountings change with repeated retellings are person-specific.

Next, we wondered \textit{how} these recountings changed over the course of the week-long delay.
The 2D projections of participants' immediate and delayed recall trajectories displayed throughout the figures above suggest---and our analyses thus far indicate---that a given participant's immediate and delayed recountings tend to be highly structurally similar.
But our analyses thus far also indicate that the delayed recountings tend to comprise slightly fewer events than their corresponding immediate recountings.
One possibility is that the events ``omitted'' from participants' delayed recountings are chosen (roughly) uniformly at random from the events they recounted immediately.
We note as a clarification here that ``omitted'' is solely a reflection of the ways participants' trajectories are segmented into discrete events, as opposed to an implication that participants are literally discretizing their memories.
Nonetheless, the segmentation procedure \textit{does} provide a way to approximate how distinct each moment of recounting is from the moments that precede and follow it.
We suggest that the number of events identified in a recounting should be interpreted as a rough approximation of the number of different ``chunks'' of content a participant is describing.

An alternative to the events ``omitted'' from participants' delayed recountings arising randomly is that there are systematic rules or principles governing which specific events tend to be omitted versus retained.
For example, that the trajectories in Figure~\ref{fig:trajectories} all appear visually similar may actually reflect something about how they compare.
In particular, suppose we take trajectory shapes (of the episode and recountings) as approximations of the true underlying sequence of ``meanings'' a participant experiences (as they are watching the original episode) or recounts (when they communicate their memories of the episode).
If the ``goal'' of encoding an experience into memory, and of communicating that memory to others, is to preserve the progression of meanings captured by the experience's trajectory through semantic space, then different events (as different coordinates in semantic space) may play distinct roles---and, critically, differ in importance---with respect to how their presence or absence affects the overall shape of their containing trajectory.

To ground this idea in physical space, consider, for example, different possible maps of a coastline.
The ``true'' shape of that coastline is nearly infinitely detailed (e.g., down to individual grains of sand, or even the subatomic level).
But in practice, the number of ``control points'' used to represent its shape is determined by the scale at which the resulting map is intended to be relevant and useful to others.
On a map intended to capture the complex topography of a single bay or estuary, that few-mile region might be defined in relatively high detail using a large number of control points.
Whereas on a more ``zoomed-out'' map encompassing the coastline of the entire containing country, that same region would instead be defined using a much smaller number of control points---and, critically, specifically the subset of points most important to faithfully preserving that region's coarse-scale shape.
This idea was first formalized in cartographic generalization (the practice of translating between maps at different scales) by the Ramer-Douglas-Peucker (RDP) algorithm~\citep{Rame72,DougPeuc73}.
Given a threshold distance $\varepsilon$, the RDP algorithm simplifies a curve composed of line segments (e.g., a map of coastline) by iteratively removing vertices within $\varepsilon$ of an approximating chord (Fig.~\ref{fig:rdp}A; for additional details see~\nameref{subsec:rdp}).
The end result is a simplified version of the original curve that preserves its high-level shape, but does so using fewer total points by discarding spatial details smaller than $\varepsilon$.
Intuitively, $\varepsilon$ thus determines the ``amount'' of simplification applied to the original curve, with progressively larger $\varepsilon$ values leading to discarding progressively coarser structure.
In this way, the smallest $\varepsilon$ value at which a given point is first removed from its containing curve indicates its importance in defining the curve's overall shape.


\begin{figure}[p]
    \centering
    \includegraphics[width=\textwidth]{figures/source/rdp-figure}
    \caption{\small \textbf{Geometrically simplifying immediate recounting predicts omissions from delayed recounting.}
    \textbf{A.} The Ramer-Douglas-Peucker algorithm applied to an example polyline.
    At each step, the algorithm draws a chord spanning the section of the polyline under consideration and identifies the interior point farthest from the chord.
    If that point is farther from the chord than the threshold distance $\varepsilon$ (blue band), it is retained (green) and the algorithm is recursively applied on either side of it.
    Otherwise, it is removed (red) with that section approximated by the chord in the simplified result.
    The simplified result (blue) preserves the high-level shape of the original but with points contributing spatial details smaller than $\varepsilon$ removed (grey).
    See \nameref{subsec:rdp} for a full description.
    \textbf{B.} An example participant's immediate (left) and delayed (right) recall trajectories, in the same format as Fig.~\ref{fig:trajectories}.
    Faded dots denote events described in immediate recall but omitted from delayed recall.
    Each trajectory was projected to 2D for visualization using UMAP; all analyses were performed in the original space.
    \textbf{C.} For each participant (pink dot), the probability that an event omitted from their delayed recounting was removed from their immediate recall trajectory at a lower $\varepsilon$ than an event retained.
    The violin shows a kernel density estimate of the distribution; the black dot and bar denote the across-participants mean and its bootstrap-estimated 95\% confidence interval.
    \textit{(Continued next page.)}
    }
    \label{fig:rdp}
\end{figure}

\begin{figure}[t]
    \ContinuedFloat
    \caption*{\small 
    \textbf{D.} Coefficients from a logistic regression predicting whether an event would be omitted post-delay from its $\varepsilon$ at removal from the episode's trajectory (grey) and the participant's own immediate recall trajectory (pink).
    Dots denote the estimated coefficients; vertical bars denote their cluster-bootstrap-estimated $95\%$ confidence intervals.
    Histograms show the null distribution of each coefficient estimated via participant-blocked permutation.
    $\varepsilon$ values were negated and $z$-scored within participants, so the coefficients express the increase in the log-odds of omission per within-participant standard-deviation increase in removability.
    \textbf{E.} The distribution of per-participant win rates.
    Each participant's win rate is the proportion of surrogate participants' immediate recountings that their own immediate recounting predicted their post-delay omissions better than.
    \textbf{F.} The proportion of participants whose own immediate recountings fall among the $k$ best predictors of their post-delay omissions, as a function of $k$.
    \textbf{C}--\textbf{F.} The dashed grey line in each panel denotes chance.}
\end{figure}

%\begin{figure}[tp]
%    \centering
%    \refstepcounter{figure}
%    \phantomsection
%    \includegraphics[width=\textwidth]{figures/source/rdp-figure}
%    \caption*{\small \textit{(Caption next page.)}}
%    \label{fig:rdp}
%    \end{figure}
%
%\begin{figure}[t]
%    \ContinuedFloat
%    \caption{\small \textbf{Geometrically simplifying immediate recounting predicts omissions in delayed recounting.}
%    \textbf{A.} The Ramer-Douglas-Peucker algorithm applied to an example polyline.
%    At each step, the algorithm draws a chord spanning the section of the polyline under consideration and identifies the interior point farthest from the chord.
%    If that point is farther from the chord than the threshold distance $\varepsilon$ (blue band), it is retained (green) and the algorithm is recursively applied on either side of it.
%    Otherwise, it is removed (red) with that section approximated by the chord in the simplified result.
%    The simplified polyline (blue) preserves the high-level shape of the original, but with points contributing spatial details smaller than $\varepsilon$ removed (grey).
%    See \nameref{subsec:rdp} for a full description.
%    \textbf{B.} An example participant's immediate (left) and delayed (right) recall trajectories, in the same format as Fig.~\ref{fig:trajectories}.
%    Faded dots denote events described in immediate recall but omitted from delayed recall.
%    Each trajectory was projected to 2D for visualization using UMAP; all analyses were performed in the original space.
%    \textbf{C.} For each participant (pink dot), the probability that an event omitted from their delayed recounting was removed from their immediate recall trajectory at a lower $\varepsilon$ than an event retained.
%    The violin shows a kernel density estimate of the distribution; the black dot and bar denote the across-participants mean and its bootstrap-estimated 95\% confidence interval.
%    \textbf{D.} Coefficients from a logistic regression predicting whether an event would be omitted post-delay from its $\varepsilon$ at removal from the episode's trajectory (grey) and the participant's own immediate recall trajectory (pink).
%    Dots denote the estimated coefficients; vertical bars denote their cluster-bootstrap-estimated $95\%$ confidence intervals.
%    Histograms show the null distributions of each coefficient estimated via participant-blocked permutation.
%    \textbf{E.} The distribution of per-participant win rates.
%    Each participant's win rate is the proportion of surrogate participants' immediate recountings that their own immediate recounting predicted their post-delay omissions better than.
%    \textbf{F.} The proportion of participants whose own immediate recountings fall among the $k$ best predictors of their post-delay omissions, as a function of $k$.
%    \textbf{C}--\textbf{F.} The dashed grey line in each panel denotes chance.}
%\end{figure}

We reasoned that, if the goal of retaining a memory across time is to preserve its high-level arc of semantic meanings despite becoming less detailed, then the differences between the paths traced out by a participant's immediate and delayed recountings might reflect a similar shape-preserving simplification.
To test this idea, we repeatedly applied the RDP algorithm to the trajectory of each participant's immediate recounting using progressively larger $\varepsilon$ values, and recorded the smallest $\varepsilon$ at which each event was discarded (i.e., its relative importance to defining the trajectory's geometric structure).
For each participant, we then computed the probability that an event they would go on to omit from their delayed recounting was removed from their immediate recounting at a lower $\varepsilon$ than one they would go on to retain (i.e., the probability of superiority), and compared these probabilities for each participant against chance.
We found that the degree to which an event could be removed from a participant's immediate recall trajectory without altering its high-level shape predicted whether that event would be omitted from their delayed recounting (mean probability of superiority = 0.576, $\text{95\% CI} = \left[0.519,~0.632\right]$, $T_W = 330$, $p = 0.007$, $r_{rb} = 0.439$; Fig.~\ref{fig:rdp}C).

We then asked whether this effect was specific to the idiosyncratic geometry of each participant's own immediate recounting.
For example, an alternative explanation for the above result could be that certain events from the episode are simply less interesting, salient, or important to the overall narrative than others, and thus both tend to contribute weakly to the geometries of participants' immediate recountings \textit{and} tend to be frequently forgotten following the delay.
We therefore performed a surrogate-based analysis testing whether an event's removability from each individual's own immediate recall trajectory predicted whether they would omit it following the delay better than its removability from \textit{other} individuals' immediate recall trajectories did.
For each participant, we computed the probability of superiority for events they omitted post-delay using those events' $\varepsilon$-at-removal values from their own immediate recounting, and from each \textit{other} participant's immediate recounting.
We then computed a ``win rate'' for each participant as the proportion of surrogate participants that their own immediate recounting's geometry predicted their omissions better than, and compared these win rates against chance.
We found that the predictive signal of events' removabilities was specific to the geometry of an individual's own immediate recounting, rather than patterns common across participants (mean win rate = 0.614, $\text{95\% CI} = \left[0.541,~0.685\right]$, $T_W = 311.5$, $p = 0.004$, $r_{rb} = 0.470$; Fig.~\ref{fig:rdp}E,F).

Given that participants' recall trajectories tend to reflect the episode's trajectory relatively faithfully (e.g., Fig.~\ref{fig:trajectories}), another alternative explanation for these results could be that an event's removability from a participant's immediate recall trajectory is simply a proxy for its removability from the episode's trajectory, and thus that omissions post-delay are really driven by properties of the stimulus, rather than a person-specific reconstruction of it.
Our earlier analyses suggest that what participants retrieve when recounting the episode after the delay is a reconstruction of their own previous recounting, rather than of the original episode itself.
If this is the case, then we should expect participants' omissions post-delay to specifically reflect a simplification of their own immediate recountings, rather than a simplification of the episode, even when accounting for both possibilities and the overlap that the episode and recall trajectories' similarities implies.
To test this claim, we fit a logistic regression to predict omissions from participants' delayed recountings based on the geometries of both their immediate recountings and the episode itself (Eqn.~\ref{eqn:regression}).
For each participant, we computed two $\varepsilon$-at-removal values for each event described in their immediate recounting: the $\varepsilon$ at which it was removed from their own immediate recall trajectory, and the $\varepsilon$ at which it was removed from the episode's trajectory.
We multiplied these $\varepsilon$ values by $-1$ (so that they denoted each event's ``removability'') and $z$-scored the events' removabilities from each trajectory, separately and within-participants, so that the two predictors' scales were comparable and their effects were estimated purely within participants (see~\nameref{subsec:rdp}).
We then used these two sets of removability values across all participants to fit a logistic regression estimating the effect of an event's removability from a participant's immediate recall trajectory on its probability of being omitted from their delayed recounting, while controlling for the analogous effect of its removability from the episode's trajectory (and vice versa).
We found that an event's removability from one's own immediate recall trajectory predicted its omission following the delay ($\beta_{\text{imm}} = 0.242$, $\text{95\% CI} = \left[0.065,~0.424\right]$, $p = 0.007$), whereas its removability from the episode's trajectory carried essentially no predictive value ($\beta_{\text{epi}} = 0.012$, $\text{95\% CI} = \left[-0.159,~0.174\right]$, $p = 0.900$; Fig.~\ref{fig:rdp}D).
Taken together, our results suggest that among the various factors that determine whether an individual will omit a part of an experience when recounting it after a delay, one such factor is the degree to which omitting it preserves versus distorts the shape of how that same individual recounted the experience previously.


\section*{Discussion}

We asked participants to view an episode of a television show, to recount what they remembered immediately afterward, and to do so again following a week-long delay.
We embedded detailed annotations of the episode, along with transcripts of each recounting, into a common semantic space, yielding a trajectory through the embedding space for the episode and for each recounting.
We segmented each trajectory into discrete events and used the resulting ``shapes'' to compare how the content of the episode and recountings unfolded over time.
Our analyses revealed that participants' delayed recountings were more similar to their own immediate recountings than to either the original episode or other participants' immediate recountings.
We also found that the delayed recountings' trajectories could be well characterized as shape-preserving simplifications of their corresponding immediate recountings' trajectories.

Taken together, our findings shed light on specifically what we do when we recount an experience, and how that process shapes our \textit{future} recountings of the same experience.
Our work builds primarily on two related literatures.
First, in our own prior work~\citep{HeusEtal21}, we developed the notion that experiences can be described as trajectories through semantic embedding space, where each coordinate along the trajectory reflects what is happening at the corresponding moment in the experience~\citep[also see][]{MannEtal22, FitzEtal26,Mann21a,Mann24}.
According to this view, recounting an experience is akin to noisily traversing that same trajectory.
Essentially, this frames recounting a past experience as a spatial navigation problem, though with some important differences.
For example, unlike navigation through physical space, navigation through semantic space need not be continuous, nor is semantic navigation constrained by the same physical limitations (e.g., in semantic space, one can ``teleport,'' so to speak, between distant coordinates from one moment to the next by abruptly shifting topics).
Nonetheless, this framework provides a powerful way of quantifying the qualitative dynamic content of an experience, facilitating comparisons across different experiences and recountings by comparing the corresponding trajectories' shapes.
Here we draw another important parallel between navigation through semantic and physical spaces.
Effectively guiding someone along a path through a physical space (e.g., giving driving directions, painting markers along a hiking trail) is often a matter of identifying a set of control points that are sufficient to guide a traveler along the intended route.
Typically these control points are chosen to resolve ambiguities at key decisions points (e.g., intersections, forks in the trail, etc.).
Whereas directions at control points are critical to guiding someone to their destination, including additional directions at other points along the route is often redundant (e.g., ``continue straight for 10 miles'' in the middle of a long stretch of highway).
If the situation calls for efficiency, such as in resource-constrained settings, then it is in both the navigator's and the traveler's interest to omit these unnecessary directions.
Similarly, if the goal of recounting an experience is to guide a listener along the same semantic path taken by one's memory of the original experience, then it is in both the speaker's and the listener's best interest to omit details that are evident from the surrounding context.
Consistent with this idea, we found that recountings after a week-long delay tend to preserve the ``control points'' of the original recounting, while omitting the ``waypoints'' that do little to change the overall shape of the semantic path.

A second line of related work concerns how memories change over time, especially with repeated recollections.
Early work from nearly a century ago~\citep{Bart32} reached (at a coarse level) a similar conclusion to ours: that repeated recountings of an experience tend to preserve the ``form'' of the original recounting (e.g., as opposed to the original experience itself).
This work was later replicated by~\cite{BergRoed99}.
Our geometric framework provides a more fine-grained and formal way of quantifying the content of an experience and its recountings, enabling us to characterize how memories change over time in a way that was not possible with prior approaches (e.g., our ``control points'' versus ``waypoints'' finding described above).
Other related work in this vein suggests that retrieving a memory pushes it into a labile state, making it more susceptible to distortion~\citep{NadeEtal00,HupbEtal07,NadeHard09,BridPall12,StJaScha13,ChanEtal09b}.
This suggests that the act of recounting an experience may itself be a mechanism by which memories change over time, as opposed to reflecting passive decay or forgetting alone.

The notion that not all events are equally memorable has also been well established in prior work, including our own.
For example, gist and central details are more likely to be encoded and retained than perceptual and peripheral details~\citep{HeusEtal21,SekeEtal16,SekeEtal18,GilbMosc21}, and autobiographical narratives shed some classes of content faster than others~\citep{DiamLevi20,SuEtal26}.
Other work has shown that memories become less perceptually detailed as they age and as they are retold~\citep{LifaEtal21}, and that events that are more central to a narrative survive better than isolated ones~\citep{LeeChen22,AntoEtal24}.
In each of these prior studies, memory distortions were characterized in terms of the content of the original experience, and the focus was on what was remembered or forgotten.
Here we are concerned with how the recountings themselves change over time, and how different recountings of the same experience relate to one another (in addition to the original experience).

Our finding that events that serve as control points are more likely to be retained than waypoint-like events contributes several additional insights into those previous findings.
One insight is that a trajectory's control points define something akin to its gist (i.e., by preserving low-spatial-frequency structure).
The present study helps to formalize this notion by providing a concrete, quantitative definition of what constitutes a control point versus a waypoint.
Another insight is that the control point versus waypoint distinction, as it pertains to persistence across recountings, is not a property of the original experience itself, but rather of the \textit{recountings} of that experience.
In our study, we did \textit{not} find evidence that control points in the original episode were more likely to be retained following the delay than its waypoints.
Only by identifying control points and waypoints in participants' immediate recountings of the episode could we identify which events were likely to be retained versus omitted in their delayed recountings.
This suggests that the control point versus waypoint distinction may not be a property of the original experience, but rather of the \textit{recountings} of that experience.
Further, because recountings are person-specific, our work suggests that the control-point versus waypoint distinction is also person-specific.
This also fits with prior work showing that people also differ in where they place event boundaries in a continuous experience~\citep{SavaEtal23}, how much of their recall structure they share with others~\citep{ChenEtal17,NguyEtal19}, the character of their autobiographical remembering~\citep{PaloEtal18}, and so on.
Within the context of the navigation analogy described above, an implication is that \textit{what is recounted} when we communicate about an experience is not purely a function of what happened.
Instead, the unique goals and perspectives we wish to convey end up shaping the way we recount it initially~\citep{TverMars00,Mars07}, which in turn subsequently shapes \textit{later} recountings as well.
This account is also consistent with prior work showing that recounting an experience can entrench the idiosyncratic information each person includes~\citep[i.e., without moving towards a common account;][]{MeleEtal25}.

Speculatively, the notion of attractor dynamics~\citep{Hopf82} may help to explain why repeated recountings will tend to resemble prior recountings more than the original experience.
Intuitively, attractor models of memory posit that laying down (i.e., encoding) a memory carves out a basin in a high-dimensional energy landscape.
Retrieving a memory then corresponds to landing somewhere within that basin.
However, retrieval has its own effect on the memory system, since ``remembering an experience'' is itself a related experience.
The experience of remembering is typically different from the original experience, in part because remembering can incorporate new information gained or retrieved \textit{after} the original experience occurred~\citep{Mann21a,Mann24}.
In this way, the representation of the experience \textit{of remembering} will differ from the representation of the original experience being remembered.
When the memory of that remembering is laid down, it carves out a \textit{new} basin in the energy landscape.
When we attempt to retrieve the original experience again, we may land either in the basin corresponding to the original experience, or in the basin corresponding to a prior remembering.
This in turn leads to laying down new memories for \textit{those} rememberings, caving out new corresponding basins in the energy landscape, and so on.
This framing suggests that the more times we remember an experience, the more likely it is that we will land in a basin corresponding to a prior remembering rather than to the original experience itself.
This offers a natural explanation for why participants' delayed recountings tend to resemble their prior recountings more than the original experience.

Whatever the underlying mechanism, our findings also suggest what might become of a memory that is recounted many times, where each retelling preserves control points and sheds waypoints, and each retelling then becomes the trajectory that the next one simplifies.
As this process repeats, memories may converge towards a skeleton of control points in the recounting's trajectory, with the intervening details (i.e., waypoints) increasingly left implied and unstated.
If future work were to test this idea using additional retellings, it would be interesting to see whether this process has a floor (beyond which further retellings do not lead to further simplification), and whether the surviving skeleton continues to resemble the original experience in any recognizable way.
These questions are worth taking seriously outside of the laboratory, since the settings in which people are asked to recount an experience repeatedly are often the settings in which accuracy matters most~\citep[e.g., eyewitness testimony; ][]{ChanEtal09b}.

We note several limitations of our work, roughly in descending order of how much they constrain our interpretations.
First, we have no means of distinguishing between what is recounted and the underlying mental representation.
It may be that the underlying memories of the episode are more stable and faithful to the original experience than participants' recountings suggest.
Or it could be that control points and waypoints are properties of \textit{storytelling} rather than of the underlying memories themselves.
To this end, we have intentionally avoided using the term ``memory'' in describing our observations of the data, and instead refer to what participants \textit{recount} from their experience.
Second, our approach to grading events as control points versus waypoints is based on a particular geometric simplification algorithm (the RDP algorithm), and other algorithms may yield different results \citep[though comparisons suggest that the RDP algorithm tends to produce the most faithful simplifications;][]{ShiCheu13}.
Third, our study used just one episode of one television show, and just one delay interval, so it remains to be seen how our findings might generalize to other experiences and delays.

The above limitations notwithstanding, one could also extend this approach to other domains.
For example, whereas we focus here on participants' recountings of a single television episode, an analogous approach could be applied to a wide range of experiences including natural conversations, academic lectures, narrations of natural phenomena, and others.
It remains to be seen whether the same principles might hold across these different scenarios.
Ultimately, by framing recountings as trajectories through semantic space, we demonstrate that repeated recountings preserve high level (coarse-scale) structure while shedding lower level (fine-scale) structure.
Our findings suggest that our memories do not simply decay over time, but rather they are actively maintained and reshaped by how we shepherd them through time by recounting them to ourselves and to others.

\section*{Methods}

Our experimental protocol was approved by the Committee for the Protection of Human Subjects at Dartmouth College.
We obtained written informed consent from all participants prior to the start of both experiment sessions.

\subsection*{Participants}

We enrolled a total of 70 Dartmouth undergraduate students in our study. 
Participants received optional course credit for enrolling and were pre-screened to ensure they had not previously seen any episode of either TV show viewed during the experiment. 
No statistical method was used to predetermine sample size. 
Prior to data processing and analysis, we excluded a total of 17 participants from our dataset: 4 encountered technical issues with the experiment, 2 self-reported difficulty with the task due to illness, 4 spoke for less than 10 minutes during the immediate recall phase, and 7 did not return for the second experiment session. 
The data we analyzed comprised the remaining 53 participants.

At the start of the experiment, we asked each participant to complete a demographic survey that included questions about their age, sex, race, and ethnicity.
Participants' ages ranged from 18 to 22 years (mean: 18.91 years; standard deviation: 1.00 years).
A total of 41 participants reported their sex as female and 12 participants reported their sex as male.
Participants reported their races as White (34 participants), Asian (16 participants), Black or African American (3 participants), American Indian or Alaska Native (1 participant), and other (1 participant).
(Note that some participants selected multiple racial categories.)
A total of 52 participants reported their ethnicity as ``Not Hispanic or Latino'' and 1 participant reported their ethnicity as ``Hispanic or Latino.''

We also asked participants their native spoken language; whether they had any hearing, speech, or color vision impairments; and about any current medications or recent injuries that could impact attention or memory.
All participants reported that their native spoken language was English, that they had no hearing or speech impairments, and that they had normal color vision.
A total of 52 participants reported no attention- or memory-relevant medications or injuries. One participant reported sustaining a concussion more than 3 years prior to the experiment and was not excluded from our dataset.

Additionally, before each of the two experiment sessions, we asked participants about their sleep, coffee consumption, and level of alertness. 
Participants reported having slept between 5 and 11 hours the night prior to session 1 (mean: 7.37 hours; standard deviation: 1.43 hours) and between 5 and 11 hours the night prior to session 2 (mean: 7.39 hours; standard deviation: 1.22 hours). 
They reported having consumed between 0 and 3 cups of coffee on the day of session 1 (mean: 0.43 cups; standard deviation: 0.71 cups) and between 0 and 3 cups of coffee on the day of session 2 (mean: 0.48 cups; standard deviation: 0.67 cups).
Participants reported their current levels of alertness on a 5-point Likert scale. At the start of session 1, 1 participant reported feeling ``Very sluggish,'' 14 reported feeling ``A little sluggish,'' 18 reported feeling ``Neutral,'' 15 reported feeling ``A little alert,'' and 5 reported feeling ``Very alert.'' At the start of session 2, 1 participant reported feeling ``Very sluggish,'' 9 reported feeling ``A little sluggish,'' 23 reported feeling ``Neutral,'' 17 reported feeling ``A little alert,'' and 3 reported feeling ``Very alert.''


\subsection*{Experiment}\label{subsec:experiment}

The experiment took place over two sessions lasting approximately one hour each, separated by 6--8 days (mean: 6.76 days; standard deviation: 0.72 days).
Prior to the start of the experiment, participants were assigned to one of two groups (\textit{A} or \textit{B}), alternating based on enrollment order.
During session 1, all participants first viewed the pilot episode of the FX series \textit{Atlanta} (``The Big Bang''; duration: 24 min 15 s).
Immediately after viewing, participants were given up to one hour to recount what happened in the episode to the best of their abilities.
Following~\cite{ChenEtal17}, participants were instructed to describe the episode in as much detail as possible and to try to speak for a minimum of 10 minutes.
They were told to try to recount the episode's events in their original order, but that they were free to go back and describe earlier events should they realize they had missed something.
After the immediate recall phase, participants were then given up to 5 minutes to predict what they thought would happen in the show's next episode.

Upon returning for session 2, all participants were again given up to one hour to recount the \textit{Atlanta} episode they had viewed the week prior.
Following the delayed recall phase, participants in Group \textit{A} then viewed the second episode of \textit{Atlanta} (``Streets on Lock''; duration: 21 min 57 s) while participants in Group \textit{B} instead viewed the pilot episode of the FOX series \textit{Arrested Development} (``Pilot''; duration: 20 min 37 s).
Both groups were then given up to one hour to recount the just-watched episode in the same manner and given the same instructions as in session 1.
The experiment was conducted in a light- and sound-attenuated testing room, with videos displayed using a 27-inch iMac desktop computer (resolution: 5120 $\times$ 2880) and sound presented using the computer's built-in speakers.
Participants were given the ability to end each recall phase at any point after the first 10 minutes by pressing a button on the keyboard when they felt they had recalled everything they could.
No participants' recountings were truncated by the one-hour time limit (i.e., all participants chose to end each recall phase within 60 minutes). 
We implemented the experiment using the psiTurk platform~\citep{GureEtal16} and jsPsych library~\citep{deLe15}, with a custom jsPsych plugin we developed in prior work for recording verbal recall audio~\citep{ZimaEtal18}.
We designed our experiment to support exploring a range of memory phenomena, including differences between immediate and delayed recall, influences of contextually congruent versus incongruent prior experience, effects of self-cuing via context reinstatement, and relationships between successful prediction and subsequent memory.
Here we analyze participants' immediate and delayed recountings of the \textit{Atlanta} episode viewed in session 1.


\subsection*{Stimulus annotation and recall transcription}\label{subsec:annotations-transcripts}

To capture the evolving semantic content of the \textit{Atlanta} episode, we created detailed text annotations of the on-screen content during each individual camera shot throughout the episode (mean annotation duration: 3.05 s; standard deviation: 1.80 s).
Specifically, we used ANVIL~\citep{Kipp01} to record, for each camera shot, a set of semantically relevant features. These included a brief description of physical occurrences and characters' explicit actions, a brief description of characters' internal states (e.g., emotions, motivations) and other implicit content, the names of all characters appearing on screen, a transcription of any dialogue, the name(s) of the character(s) speaking, the shot's setting, whether the shot took place indoors or outdoors, and whether or not there was music playing during the shot.
This feature set was adapted from a similar annotation corpus created by~\cite{ChenEtal17}, which we used in developing our trajectory-analysis framework in prior work~\citep{HeusEtal21}.

We then transcribed each participant's immediate and delayed verbal recountings of the \textit{Atlanta} episode.
Our transcription process consisted of three steps: an initial manual transcription, a two-pass prose-formatting conversion with a large language model, and a final manual cleanup and correction step. 
First, we manually transcribed each recall audio file collected during the experiment.
These initial transcripts reflected participants' spoken recountings verbatim, without any punctuation or additional formatting, and including disfluencies, stuttered repetitions, and other artifacts common to unrehearsed speech.
However, since transformer models (such as we used to obtain text embeddings of participants' recalls) are trained almost exclusively on written text, structural conventions like sentence breaks, punctuation, and capitalization provide an important signal these models leverage to contextualize other tokens (e.g., words) and construct a representation of the text's overall meaning.
Meanwhile, speech-native phenomena such as stuttered repetitions and filler words (``like'', ``um'', etc.) that are comparatively rare in written text contribute out-of-distribution noise that can be expected to degrade embedding fidelity, and prior work has shown that automated removal of these phenomena improves performance on downstream tasks that assume clean, written-style input~\citep{ChenEtal22,WangEtal10b}.
We therefore used a large language model~\citep[Qwen3.5 122b;][]{Qwen26} to convert participants' recall transcripts to a structure and style more typical of written text.
We used the \texttt{langchain\_dartmouth} Python package~\citep{StonEtal25} to implement this conversion as two sequential passes over each transcript.
In the first pass, we prompted the model to split the transcript into sentences at natural breaking points, add punctuation including quotes around reported speech, remove clear non-word disfluencies (``um,'' ``uh,'' ``hmm,'' etc.), collapse stuttered verbatim repetitions, and mark self-corrections and restarts with em-dashes.
In the second pass, we prompted the model to remove all instances of ``like'' used as a filler word.
We removed filler ``like'' instances in a separate pass because of their high frequency of occurrence in the transcripts and the additional effort to required to distinguish them from meaningful uses of ``like.''
The full texts of our prompts for both passes can be found in \suppMethods.
Finally, we manually reviewed all LLM-formatted transcripts to identify and correct any errors, ensure sentence breaks were appropriate and consistent across participants and sessions, confirm that no content was improperly added to or removed from the transcripts, and resolve any ambiguities based on the prosody of participants' original recall audio.


\subsection*{Analysis}

\subsubsection*{Statistics}\label{subsec:stats}

All statistical tests we performed were two-sided. All tests we performed were nonparametric, with the exception of the logistic regression analysis, for which the model itself is necessarily parametric but we estimated confidence intervals and $p$-values in a nonparametric manner (see~\nameref{subsec:rdp}).
%Because our analyses are performed on scales whose values are bounded and may not be normally distributed (e.g., order-similarity, warp cost, $\varepsilon$-at-removal), 
For all other statistical comparisons, we used Wilcoxon's signed-rank test~\citep{Wilc45}.
Given two distributions of paired observations, the Wilcoxon signed-rank statistic $T_W$ is computed by first taking the set of paired differences, discarding any that are exactly zero, and ranking the remaining $m$ differences by their absolute values (the smallest absolute difference is assigned rank 1; the largest is assigned rank $m$). 
For the one-sample Wilcoxon signed-rank test, the observations are analogously ranked by their absolute differences from the null-hypothesized center.
In both cases, the resulting ranks are then divided into two groups by their corresponding differences' signs, and the ranks in each group summed (where $W^+$ denotes the sum of the positive differences' ranks and $W^-$ denotes the sum of the negative differences' ranks).
The two-sided test statistic $T_W$ is then given by the \textit{smaller} of $W^+$ and $W^-$.
For a given directional effect, the test statistic thus denotes the weight of the rank-sum evidence in the \textit{opposite} direction.
We note that this means the test statistic's magnitude varies inversely to that of analogous parametric tests (e.g., $t$-tests): for example, in a case where every observation exhibited a shift in the same direction, the value of $T_W$ would be 0.
Under the null hypothesis that each difference is equally likely to be positive or negative, the $p$-value for the test statistic is then computed (via the ``exact'' method, which we use here) by enumerating all $2^m$ possible sign-assignments for the $m$ differences' ranks, re-deriving the test statistic (as the smaller of $W^+$ and $W^-$) for each assignment, and computing the proportion of sign-permuted statistics at least as extreme as (i.e., less than or equal to) the observed statistic.
Because $T_W$ is not itself an interpretable measure of effect size (and its magnitude is perhaps somewhat counterintuitive), alongside each test we also report the rank-biserial correlation \citep[computed as $\frac{W^+ - W^-}{W^+ + W^-}$;][]{Kerb14}, which ranges from $-1$ to $+1$, grows with the strength and consistency of the effect, and shares an exact $p$-value with its corresponding signed-rank test.
The 95\% confidence intervals we report alongside each test were obtained by subsampling participants (with replacement) 10,000 times, recomputing the measure of interest for each subsample, and identifying the 2.5\textsuperscript{th} and 97.5\textsuperscript{th} percentiles of the resulting bootstrap distribution.


\subsubsection*{Constructing episode and recall trajectories}\label{subsec:text-embedding}

We adapted a method we developed in prior work~\citep{HeusEtal21} to transform the \textit{Atlanta} episode and participant's recountings of it into trajectories through a common representation space using a text embedding model.
Briefly, text embedding models take in natural language text of an arbitrary length and output a fixed-length vector representation that captures the text's semantic meaning.
Intuitively, an output vector of length $n$ can be viewed as a coordinate in an $n$-dimensional text embedding space.
While specific model architectures and training paradigms vary, modern text embedding models are broadly trained to output similar coordinate vectors (in terms of cosine similarity, dot product, inverse Euclidean distance, or other geometric measures) for semantically similar input texts, even when those texts share few or no specific words.
We obtained text embeddings for the episode and recountings using EmbeddingGemma~\citep{VeraEtal25}, a pre-trained 308-million-parameter encoder-only transformer model that outputs coordinate vectors in a 768-dimensional embedding space.

To construct the trajectory for the \textit{Atlanta} episode, we first converted our tabular annotations of the content present during each camera shot to a plain-text format.
Separately for each shot, we concatenated the annotation text describing explicit on-screen occurrences and characters' implicit internal states.
We then appended ``Speech:~'' followed by the shot's dialogue, ``Character speaking:~'' followed by the name of the speaker, and ``Setting:~'' followed by the shot's setting.
If any annotated fields were empty for a given shot (e.g., if no character was speaking at that time), we omitted them and their prefixes from the concatenated text.
Next, we used EmbeddingGemma to transform the text snippet for each camera shot into a text embedding vector, resulting in a number-of-shots (478) by number-of-features (768) matrix.
Since the camera shots varied in duration, we then resampled this series of embedding vectors to a consistent temporal resolution.
We assigned each vector to the timepoint midway between the start and end of its corresponding camera shot (in seconds) and used linear interpolation to estimate a vector for each second of the episode.
This yielded a number-of-seconds (1,455) by number-of-features (768) matrix---i.e., a 768-dimensional \textit{trajectory}.

We then constructed similar trajectories for each participant's immediate and delayed recountings of the episode.
We began by splitting each recall transcript into a list of sentences, with the constraint that splits could not occur within reported speech (i.e., if a participant recounted a multi-sentence quote by a character in the episode, we treated the full quote as a single sentence-unit).
We then parsed these sentences into overlapping sliding windows that spanned up to 5 consecutive sentences each.
These windows grew to and shrank from their full size at the beginning and end of a participant's recounting---in other words, the first window contained only the first sentence of the participant's recounting, the second contained the first two sentences, and so on.
This ensured that all sentences from a participant's recall transcript appeared an equal number of times (5) in their sequence of sliding windows.
Finally, we used EmbeddingGemma to transform each sliding window into a text embedding vector.
Repeated for all recall transcripts, this yielded a separate trajectory characterizing each participant's recounting from each of the two experiment sessions. The number of samples in participants' recall trajectories (i.e., the number of sliding windows parsed from their recall transcripts) ranged from 61 to 308 (mean: 136.68; standard deviation: 49.70).

\subsubsection*{Relating episode and recall embedding vectors}

In prior work using our trajectory-analysis framework~\citep[e.g.,][]{HeusEtal21,Mann21a,MannEtal22,RaccEtal24,FitzEtal26}, embeddings of the stimulus and participants' responses were constructed using a topic model~\citep[Latent Dirichlet Allocation;][]{BleiEtal03} trained solely on the stimulus content.
Here we elected to instead use a pre-trained transformer-based text embedding model due to the richer and more nuanced textual information these models are able to capture. (For example, these context-sensitive models can disambiguate the texts ``Earn looked at Alfred'' and ``Alfred looked at Earn'' in their representations, whereas bag-of-words models like LDA would represent the two identically.)
However, these richer representations also introduce certain geometric nuisances we needed to account for in order to effectively relate the contents of participants' recountings to that of the episode they viewed.
First, because modern transformer models are pre-trained on large and diverse text corpora, the representational capacities of their embedding spaces span an enormous breadth of semantic content.
Consequently, in preliminary analyses we found that the embeddings of our annotations and participants' recountings (which were all ``about'' the events, themes, and characters of a single \textit{Atlanta} episode) were strongly anisotropic---i.e., they all occupied the same relatively narrow angular region of the model's overall embedding space.
Additionally, because transformer models are sensitive to the syntactic structure of text, their embedding representations carry latent information about the text's linguistic register in addition to its semantic content.
Accordingly, we found there was a consistent angular offset between embeddings of the episode annotations and those of participants' recountings, reflecting the fact that the annotations comprise grammatically well-structured written text while the recountings comprise transcripts of casual unrehearsed speech.

We corrected for these representational artifacts by mean-centering the episode and recall trajectories before performing analyses that involved measuring similarities between embedding vectors within or across trajectories.
Conceptually, mean-centering a given set of embedding vectors (i.e., subtracting the across-vector average from each individual vector) serves to remove representations that are common across all vectors under consideration, and instead highlight representations that \textit{vary} between them.
For example, consider the sequence of embedding vectors comprising the trajectory of the \textit{Atlanta} episode.
Common across vectors within this sequence will be latent signals driven by how we chose to format the annotation text passed to the embedding model, the annotator's particular idiolect (i.e., idiosyncratic vocabulary and writing style), and any high-level overarching themes of the episode.
\textit{Not} common across these vectors are the finer-grained semantic representations that distinguish different events, scenes, and actions throughout the episode from each other.
We found that removing these non-discriminative signals (to the extent they behave as additive offsets) from the episode and recountings via mean-centering helped reduce the embeddings' anisotropy (i.e., increased the range of measured between-vector similarities) and improved our ability to distinguish different temporal periods of semantic content within a single trajectory.
Similarly, mean-centering both the episode and recountings before relating their embedding representations helped mitigate broad-scale representational differences driven by their diverging registers, and enabled us to better relate them based on event-specific, content-driven patterns.

Our approach to mean-centering the recall trajectories was guided by the particular latent representations we wanted to remove versus retain.
In segmenting each participant's recall trajectory from each session into events (see \nameref{subsec:eventseg}), our goal was to discriminate between embedding vectors strictly within a single recall sequence in order to identify temporally transient content-related patterns.
We therefore centered each recall trajectory (in addition to the episode) on its own centroid to remove non-discriminative representations (e.g., register, broad-scale thematic content) and maximize the contrast between vectors within that single sequence.
However, the analyses shown in Figures~\ref{fig:dtw} and \ref{fig:rdp} entailed comparing sequences of recall events not only to the episode, but also to each other, both within- and across-participants, to examine whether within-participant similarities tended to exceed across-participant similarities. 
Here our goal was to remove representational artifacts that could work to bias our results towards significance.
Specifically, participants' idiolects (i.e., particular speaking styles and sets of words with which they describe the episode) will influence the embedding representations of their recalls in ways that tend to be similar between their immediate and delayed recalls, but differ across participants.
To correct for this, we jointly centered each participant's set of immediate and delayed recall events on the two sequences' mean centroid (i.e., the average of the two event-sequences' centroids, weighting immediate and delayed recall equally). 
This served to reduce the recall embeddings' anisotropy (by removing signals driven by high-level themes of the episode they recounted) and mitigate the effects of the spoken-recall register and participant-specific idiolect (both of which are shared between a participant's recountings from the two sessions) while preserving any genuine drift in the high-level content of each participant's recounting over the delay (which per-session mean-centering would instead discard).


\subsubsection*{Event-segmentation}\label{subsec:eventseg}

After constructing trajectories for the episode and each participant's immediate and delayed recounting, we used hidden Markov models~\citep[HMMs;][]{BaumPetr66,Rabi89} to segment each trajectory into a sequence of discrete events.
Given a number of states $K$, an HMM models an observed time series as a progression through $K$ hidden states, each of which has a characteristic signature in the data (i.e., a pattern that timepoints belonging to that state tend to exhibit) and tends to persist across timepoints with occasional transitions rather than fluctuating freely.
Because states persist, inferring the most probable state at each timepoint segments the observed time series into a sequence of events---i.e., spans of contiguous timepoints with the same most-probable state.

We segmented the episode trajectory into events using the HMM implemented in the BrainIAK toolbox~\citep{KumaEtal21}, which jointly learns a mean pattern for each of $K$ states and the set of transitions between them via the Baum-Welch algorithm~\citep{BaumEtal70}, with the added constraint that each state is visited exactly once in a strict left-to-right order~\citep{BaldEtal17}.
To select an appropriate number of states (i.e., events) for the episode, we used an optimization procedure we developed in prior work~\citep{HeusEtal21} that aims to identify the single timescale of event representations emphasized most heavily in the episode's narrative structure.
We began by segmenting the episode trajectory into $K$ events for each $K$ between 2 and 50, inclusive.
For each candidate segmentation, we then computed the correlation between the embedding vectors for each pair of timepoints assigned to the same event, and for each pair of timepoints assigned to different events.
When constructing these distributions of within- and across-event correlations, we restricted comparisons to pairs of timepoints separated by no more than the duration of the longest event in the candidate segmentation.
This ensured that the within- and across-event distributions were computed over comparable temporal distances and the across-event distribution was not dominated by distant timepoint pairs that we would expect to be dissimilar regardless of event structure.
We then selected the candidate segmentation that maximized the Wasserstein distance between these within- and across-event distributions.
We found that the maximally distinctive and internally self-similar segmentation was yielded by $K = 20$ events (see Supp.~Fig.~\kOptimization).
Finally, we averaged the embedding vectors over timepoints between each pair of adjacent event boundaries to obtain a sequence of stable event-average representations for the episode.

To segment participants' recall trajectories into sequences of events, we developed a novel HMM-driven procedure that grounded participants' recountings directly against the content of the episode they viewed.
Essentially, rather than re-learning separate sets of state representations from each participant's recountings (which tend to be shorter, noisier approximations of the episode content), we leveraged the state representations already learned from the episode to identify transitions between the episode events a given participant was (most probably) describing at a given point during their recounting.
First, we mean-centered the sequence of event-average embedding vectors for the episode, and each participant's sequence of per-window embedding vectors from each session, separately.
For each given recall trajectory, we then computed the cosine similarity between the embedding vector for each recall window and the event-average embedding vector for each of the 20 episode events.
This yielded a number-of-windows by number-of-episode-events (20) similarity matrix.
We then treated the episode events as the hidden states of a hidden Markov model with fixed parameters, and treated each recall window's profile of similarities to the episode events, $z$-scored across all window-event pairs, as its distribution of emission scores over states.
We imposed an ergodic transition structure (i.e., we allowed transitions from any state to any other state, since participants may recount the episode events out of order, and allowed states to be re-entered multiple times, since participants may revisit earlier events in their recountings) governed by separate additive transition penalties for forward-one transitions and all other transitions.
Specifically, we imposed no penalty for remaining in the current state, a small penalty (1.5 normalized emissions units) for transitioning from a given state to the next (i.e., transitioning from recounting episode event $n$ to recounting episode event $n + 1$), and a larger penalty (3.0 normalized emissions units) for all other transitions.
%We note that $z$-scoring each recall trajectory's event-similarity profile ensured these transitional penalties carried a constant weight across recall trajectories with different lengths and overall similarities to the episode
Finally, we decoded the most probable sequence of episode-event labels using the Viterbi algorithm~\citep{Vite67}, collapsed runs of consecutive identically labeled recall windows into events, and averaged the embedding vectors over windows within each event.
This procedure yielded, for each participant's recounting from each session, a sequence of event-average embedding vectors and a sequence of labels denoting the episode event to which each recall event corresponded.

\subsubsection*{Comparing recall orderings}\label{subsec:precedence}

In the analyses shown in Figure~\ref{fig:recall-order}, we asked whether idiosyncratic patterns in the order in which participants recounted the episode's events immediately after viewing were preserved following the week-long delay.
To quantify these patterns and compare them between recountings, we developed a measure based on the ordinal ``precedence'' of different episode events within a given recounting.
Specifically, for each pair of episode events $X$ and $Y$ that a given participant recalled, and where event $X$ preceded event $Y$ in the original episode, we computed $\mathrm{precedence}(X, Y)$ as the proportion of times a recall event matching episode event $X$ appeared before a recall event matching episode event $Y$.
For example, if a given participant recalled event $X$ before event $Y$, $\mathrm{precedence}(X, Y)$ would be $1$. If they transposed events $X$ and $Y$ in their memory (i.e., recalled $Y$ before $X$), $\mathrm{precedence}(X, Y)$ would be $0$. If they inserted the content of event $Y$ partway through their recounting of event $X$, or revisited event $X$ later on after recalling event $Y$, $\mathrm{precedence}(X, Y)$ would be $0.5$.
We computed the order agreement between two different recountings of the episode as the mean absolute difference between precedence scores across all pairs of events that appeared in both recall sequences. 

We performed a surrogate-based analysis that tested whether the ordering structure of a participant's delayed recounting tended to reflect their own immediate recounting to a greater degree than it did \textit{other} participants' immediate recountings.
We computed the order agreement between each participant's delayed recounting and their own immediate recounting, and between their delayed recounting and each other participant's immediate recounting.
We then computed the percentile rank of each within-participant order agreement against its distribution of across-participant order agreements, and performed a one-sample Wilcoxon test comparing these per-participant percentile ranks against a null rank of 0.5.


\subsubsection*{Comparing the geometric structures of the episode and recalls}\label{subsec:dtw}

We compared the geometric structures of the episode and recall event-sequences using dynamic time warping~\citep[DTW;][]{Vint68,SakoChib78}.
The DTW algorithm finds the optimal order-preserving alignment between two sequences of vectors with potentially different lengths or sampling rates by locally ``stretching'' and ``compressing'' them in time to maximize their overall similarity.
This alignment yields a ``warp path'' that maps each vector in each sequence onto one or more consecutive vectors in the other sequence, minimizing the total distance between matched vectors.
The summed or average distance between matched vectors along the optimal warp path (the ``warp cost'') is commonly taken as a measure of the two sequences' structural dissimilarity.
We computed the warp cost for a given pair of event-sequences as the average cosine distance between DTW-matched events so that these costs would be comparable across comparisons involving event-sequences of different lengths (whereas summed distance necessarily grows with sequence length).

We hypothesized that the idiosyncratic geometric structures of participants' delayed recountings tended to reflect the structures of their immediate recountings.
In the analyses shown in Figure~\ref{fig:dtw}, we tested this hypothesis two different ways.
First, we analyzed whether participants' delayed recall event-sequences tended to be more geometrically similar to their own immediate recall event-sequences than to the episode's event-sequence.
We began by mean-centering the episode event sequence, and centering each participant's immediate and delayed recall event sequences by their shared centroid.
For each participant, we then computed the warp cost of aligning their delayed recall event-sequence to their immediate recall event-sequence, and the warp cost of aligning their delayed recall event-sequence to the episode's event sequence.
We then performed a Wilcoxon signed-rank test comparing the resulting paired distributions of delayed-to-immediate and delayed-to-episode costs to assess whether the two systematically differed.

Second, we analyzed whether participants' delayed recall event-sequences tended to be more geometrically similar to their own immediate recall event-sequences than they were to \textit{other} participants' immediate recall event-sequences.
Here we used a surrogate-driven approach analogous to the approach we used to compare recall orderings.
For each participant, we computed the warp cost of aligning their delayed recall event-sequence to their own immediate recall event-sequence, and the warp cost of aligning their delayed recall event-sequence to every other participant's immediate recall event-sequence.
We then computed the percentile rank of each within-participant warp cost against its distribution of across-participant warp costs and performed a one-sample Wilcoxon test comparing these per-participant percentile ranks against a null rank of 0.5.
Since either set of recountings may be treated as the targets and the other as the pool to be searched, we also ran this comparison in the reverse direction.
We computed the warp cost of aligning each participant's immediate recall event-sequence to their own delayed recall event-sequence and to every other participant's delayed recall event-sequence, percentile-ranked each within-participant cost against its distribution of across-participant costs, and tested the resulting ranks against a null rank of 0.5.

We also asked whether participants' recountings aligned less closely with the episode following the delay.
Here we computed, for each participant, the warp cost of aligning their immediate recall event-sequence to the episode's event-sequence, and the warp cost of aligning their delayed recall event-sequence to the episode's event-sequence, and compared the resulting paired distributions via a Wilcoxon signed-rank test.


\subsubsection*{Predicting forgetting through geometric simplification}\label{subsec:rdp}

In the analyses shown in Figure~\ref{fig:rdp}, we asked whether the particular episode events a participant would go on to omit from their delayed recounting could be explained by geometrically simplifying the path through semantic space traversed by their immediate recounting.
We formalized this geometric simplification using the Ramer-Douglas-Peucker (RDP) algorithm~\citep{Rame72,DougPeuc73}.
Originally developed for use in cartographic generalization, the RDP algorithm iteratively simplifies a piecewise curve composed of line segments (e.g., a high-resolution map of a coastline) to a similar curve defined by fewer points (e.g., a lower-resolution map of the same coastline).
Briefly, given a curve and a tolerance distance $\varepsilon$, the RDP algorithm works by drawing a chord connecting the first and last points along the curve, and identifying the interior point along the curve farthest from the chord.
If the distance from the chord to that target point is less than $\varepsilon$, then all points along the curve between the ends of the chord are discarded, and that section of the curve (which, in the initial step, comprises the full curve) is approximated by the chord in the simplified result.
Alternatively, if the target point is farther than $\varepsilon$ from the chord, it is retained in the simplified result and the algorithm is recursively applied to the sections of the curve on either side of the retained point.
The end result is a simplified curve that preserves the high-level shape of the original, but with all spatial details smaller than $\varepsilon$ removed.
Intuitively, the $\varepsilon$ parameter determines the ``amount'' of simplification applied to the original curve: smaller values of $\varepsilon$ result in discarding only fine-grained details, while larger values lead to discarding more coarse structure.
In this way, points along a path that the RDP algorithm discards at lower values of $\varepsilon$ may be thought of as ``waypoints'' that contribute relatively little to defining to the paths's overall shape, while points discarded only at higher $\varepsilon$ values may be thought of as ``control points'' that contribute more substantially.

We hypothesized that the differences between the semantic paths traversed by a participant's immediate and delayed recall event-sequences might reflect a similar shape-preserving simplification.
To test this idea, we first asked whether the $\varepsilon$ value at which the RDP algorithm discards a participant's recall of a given episode event predicted whether they would omit that event from their delayed recounting.
We applied the RDP algorithm to each participant's sequence of immediate recall events over a sweep of $\varepsilon$ values spanning the range of possible cosine distances between the events' embedding vectors (i.e., from 0 to 2; increasing $\varepsilon$ by 0.1 each iteration).
For each episode event a given participant recalled, we identified the minimum $\varepsilon$ at which its matched recall event was discarded from their event sequence.
We then computed the proportion of times an event the participant omitted from their delayed recounting was removed at a lower $\varepsilon$ value than an event they did not (i.e., the probability of superiority, or equivalently, AUC).
We repeated this process for all participants who omitted at least one immediately recalled event from their delayed recounting (48 out of 53) and performed a one-sample Wilcoxon test comparing the resulting distribution of proportions against a null of 0.5. Because the RDP algorithm cannot remove the endpoints of a curve, we excluded the first and last episode event each participant recalled from their data for this analysis. 
When a given episode event matched more than one event in a participant's immediate recounting, we used the lowest $\varepsilon$ value at which a matching recall event was discarded, and counted the event as omitted if it was not matched by multiple events in the participant's delayed recounting (this removed a total of 38 duplicate matches out of 861 non-endpoint recall events, across all participants).

We then examined whether this simplification was specific to the geometry of each individual's immediate recounting, rather than driven by general patterns in forgetting.
We again used a surrogate-driven procedure, here to test whether the events an individual omitted in their delayed recounting were better predicted by the simplified geometry of their own immediate recounting than by that of other participants' immediate recountings.
For a given target participant $A$ and a given surrogate participant $B$, we computed the probability of superiority for the events participant $A$ omitted post-delay using the $\varepsilon$ values at which each event was removed from $A$'s immediate recounting and $B$'s immediate recounting, separately, and considering only the events that both participants recounted in the immediate session.
We repeated this across all surrogates for the target participant, and computed the proportion of target-surrogate comparisons in which the probability of superiority derived from the target participant's own recall geometry was greater than that derived from the surrogate participant's recall geometry.
Repeated for all target participants, this yielded a set of per-participant ``win rates'', which we compared via a one-sample Wilcoxon test against a null rate of 0.5. 
We restricted the pool of surrogate participants for each target participant to those participants who recalled at least one event in the immediate session that the target participant omitted in the delayed session (in order to calculate probabilities of superiority; this excluded 175 total surrogates across 2,496 target-surrogate pairs).

Finally, we asked whether participants' post-delay omissions were predicted specifically a simplification of their own immediate recall geometry rather than by a simplification of the original episode's geometry.
We performed a regression analysis that tested the effect of an event's RDP-removability from a participant's immediate recall geometry on its probability of being omitted post-delay, while controlling for the analogous effect of its RDP-removability from the \textit{episode's} geometry.
For each participant, we computed two ``$\varepsilon$ at removal'' scores for each episode event they recounted in the immediate session: the $\varepsilon$ at which it was discarded from their immediate recall event-sequence, and the $\varepsilon$ at which it was discarded from the episode's event-sequence.
As above, since the RDP algorithm cannot remove the endpoints of a curve, we excluded the first and last episode event and, if they differed, the episode events matched by the participant's first and last immediate recall events.
We then multiplied these ``$\varepsilon$ at removal'' scores for each episode event by $-1$ so that they represented ``removability'' (i.e., higher scores $\Rightarrow$ more RDP-removable), and $z$-scored the events' removability scores from each source, separately, so their scales were comparable both with each other and across participants.
We then used these two $z$-scored removability scores across all episode events recounted by all participants in the immediate session to fit a logistic regression:
\begin{equation}\label{eqn:regression}
  \operatorname{logit}\!\big(\Pr(\text{omitted}_{pe}=1)\big)
    = \beta_0 + \beta_{\text{imm}}\, z^{\text{imm}}_{pe} + \beta_{\text{epi}}\, z^{\text{epi}}_{pe}
\end{equation}
where $\operatorname{logit}(q) = \log\!\big(q/(1-q)\big)$, $\text{omitted}_{pe}$ indicates whether episode event $e$ recounted in the immediate session by participant $p$ was omitted ($1$) or retained ($0$) in their delayed recounting, and $z^{\text{imm}}_{pe}$ and $z^{\text{epi}}_{pe}$ are the within-participant $z$-scored removability scores of that event computed from participant $p$'s own immediate recall event-sequence and from the episode's event-sequence, respectively.
The coefficients $\beta_{\text{imm}}$ and $\beta_{\text{epi}}$ thus express the change in the log-odds of omission per within-participant standard-deviation increase in removability, with intercept $\beta_0$.
We note that because the within-participant $z$-scoring removes each participant's baseline and scale in removability scores, the estimated coefficients reflect purely within-participant variation and cannot be driven by between-participant differences in removability.

To estimate $p$-values for these coefficients, we used a participant-blocked permutation procedure.
For each of 10,000 permutations, we randomly shuffled the ``omitted'' labels within each participant's set of immediately recounted episode events, independently, and preserving the numbers of episode events they omitted versus retained in following the delay.
We then re-fit the above model using each set of permuted labels to construct a null distribution of coefficients, and computed a $p$-value as $\frac{r+1}{n+1}$, where $r$ is the number of permutations that yielded a coefficient whose absolute value was greater than or equal to that of the observed coefficient, and $n$ is the number of permutations we performed (10,000).

To construct the 95\% confidence intervals displayed in Figure~\ref{fig:rdp}D, we used a cluster bootstrap procedure.
For each of 10,000 bootstraps, we subsampled participants (with replacement) from the full pool and re-fit the above model using each subsample's data. 
This yielded bootstrap distributions of 10,000 $\beta_{\text{imm}}$ and $\beta_{\text{epi}}$ coefficients, from which we computed the 2.5\textsuperscript{th} and 97.5\textsuperscript{th} percentiles.


\section*{Data availability}

All of the data analyzed in this manuscript may be found at
https://github.com/Con\-text\-Lab/memory-dynamics.


\section*{Code availability}

All of the code for running our experiment and carrying out the analyses may be
found at https://github.com/Con\-text\-Lab/memory-dynamics.


\bibliographystyle{apa}
\bibliography{CDL-bibliography/cdl}


\section*{Acknowledgements}

We thank Luke Chang, Claudia Gonciulea, Andrew Heusser, Lucy Owen, Xinming Xu, and Kirsten Ziman for useful discussions.
Our work was supported in part by NSF CAREER Award Number 2145172 to J.R.M.
The content is solely the responsibility of the authors and does not necessarily represent the official views of our supporting organizations.
The funders had no role in study design, data collection and analysis, decision to publish, or preparation of the manuscript.


\section*{Author contributions}

Conceptualization: P.C.F. and J.R.M. Methodology: P.C.F. and J.R.M. Software:
P.C.F. Validation: P.C.F., A.T., J.X., and J.R.M. Formal analysis: P.C.F., A.T.,
J.X., and J.R.M. Investigation: P.C.F. Resources: J.R.M. Data curation: P.C.F. and S.H.P.
Writing (original draft): P.C.F. and J.R.M. Writing (review and editing):
P.C.F., A.T., J.X., S.H.P, and J.R.M. Visualization: P.C.F. Supervision: J.R.M. Project
administration: P.C.F. and J.R.M. Funding acquisition: J.R.M.


\section*{Competing interests}

The authors declare no competing interests.


\end{document}
