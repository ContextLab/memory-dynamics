# memory-dynamics
Binder for memory-dynamics project

# To use:
1. `git clone https://github.com/ContextLab/memory-dynamics.git`
2. navigate to the folder and then `pip install -r requirements.txt`
3. then `jupyter notebook memory-dynamics.ipynb`
